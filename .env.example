# canvas-transcriber environment variables
# Copy this file to .env and fill in values as needed.
# All variables are optional — defaults are shown below.

# Path to the session cookie file (created after first login)
# CT_SESSION_FILE=session.json

# Path to the extracted links JSON (produced by extract-page / crawl-course)
# CT_LINKS_FILE=links_output.json

# Directory where transcript .txt files are saved
# CT_OUTPUT_DIR=transcripts

# Seconds to wait for the user to complete SSO/MFA login
# CT_LOGIN_TIMEOUT=180

# Set to "true" to run the browser in headless mode (no visible window).
# Leave unset or "false" for the default non-headless mode, which is
# required for schools that use SSO / MFA.
# CT_HEADLESS=false
